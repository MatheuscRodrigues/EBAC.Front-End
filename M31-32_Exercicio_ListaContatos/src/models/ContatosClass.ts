class Contatos {
  id: number
  nome: string
  contato: string
  email: string

  constructor(id: number, nome: string, contato: string, email: string) {
    this.id = id
    this.nome = nome
    this.contato = contato
    this.email = email
  }
}

export default Contatos
